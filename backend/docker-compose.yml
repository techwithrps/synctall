version: '3.8'

services:
  backend:
    build: .
    container_name: tally_integration_backend_node
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always
    volumes:
      - .:/workspace
      - /workspace/node_modules
